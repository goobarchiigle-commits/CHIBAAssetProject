"""src/research — 戦略研究・最適化モジュール。

Submodules:
  contracts/    — immutable canonical interfaces (Phase A)
  walkforward/  — deterministic fold generation + artifacts (Phase B)
  orchestrator/ — experiment registry, queue, scheduler, worker (Phase C)
  regime/       — rule-based regime labeling (Phase D)
  evaluation/   — regime-aware metrics, rejection, ranking (Phase E)
"""
