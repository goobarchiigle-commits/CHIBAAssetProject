# 2026-07-17 研究ログ — Entry Freeze Mode（資産保全）実装

## タスク
Study100/101の帰結（RSR42 selection alpha=FATAL・旧フジコ法=ベンチマーク不適格）を受け、
研究継続中の資産保全として新規BUY発注のみを全面停止するEntry Freeze Modeを実装。
DRY/LIVE両パイプラインは現状維持で継続稼働・SELL/signal generation/diagnosticsは無停止。

## 実装
- `src/configs/strategy.yaml` + `src/config_loader.py`: `entry_freeze`設定追加（既定=無効・
  環境変数`ENTRY_FREEZE_ENABLED`で緊急上書き可）
- `src/kabusapi/signal_bridge.py::_build_orders()`: 既存Circuit Breaker早期returnをOR結合で流用
- `run_live_signal.py`・`run_morning_signal.py`: SignalBridge生成箇所へ配線
- `src/execution/live_pipeline.py::generate_orders()`: 全探索で発見した独立発注経路にも同一ゲート追加

## 全BUY経路探索の結果
正典経路（run_live_signal.py）以外に、SignalBridgeを一切経由しない独立した決定論的
リバランサー（`pipeline.py`無引数実行→`live_pipeline.py::execute_orders()`→sendorder直呼び）
が存在すると判明。`KABU_API_KEY`未設定のため現状デッドだが将来のリスクとして同一fixを適用。
`_send_orders_with_retry`・`KabusApiAdapter.submit_order`は呼び出し元ゼロのデッドコードと確認。
manual_order.py/recovery scripts/scheduler jobsにBUY経路なしと確認。

## 検証
新規テスト18件全合格（Test1: BUY blocked DRY/LIVE一致・Test2: SELL通常実行・Test3相当:
CB+freeze同時発動でも契約破綻なし）。副次的に発見した既存テスト2件のモック回帰を修正。
`src/kabusapi/`58件・`src/live/`+`src/execution/`123件、回帰なし。

## 成果物
- `reports/entry_freeze_mode_2026-07-17.md`（freeze実装箇所・残存BUY経路・検証結果・rollback手順）
- `src/kabusapi/test_entry_freeze.py`
- `src/test_config_loader_entry_freeze.py`
- `src/execution/test_live_pipeline_entry_freeze.py`

## 現状
`entry_freeze.enabled=true`でcommit（ユーザー確認の上「今すぐ有効化」を選択）。
**新規BUYは既に全面停止中**。解除にはユーザーの明示操作（yaml編集 or `ENTRY_FREEZE_ENABLED=0`）が必要。

## 未実施
git push（ASK_FIRST）
