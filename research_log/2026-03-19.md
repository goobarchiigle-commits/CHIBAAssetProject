# 研究ログ 2026-03-19

## 実施内容

### バイアス定量化（時間的分離バックテスト）
**スクリプト**: `backtest/portfolio_temporal_separation.py`（新規作成・実行済み）

#### 設計
- Selection期間: 2015-01-01〜2017-12-31（この期間のみで銘柄選択）
- Eval期間:      2018-01-01〜2024-12-31（portfolio評価）
- フィルタ閾値: Sharpe>0.3 / MaxDD>-30%（再最適化禁止）

#### 結果

| シナリオ | Sharpe | CAGR | MaxDD | Calmar |
|---|---|---|---|---|
| BIASED（現行・in-sample） | 1.724 | +16.42% | -8.32% | 1.973 |
| **TEMPORAL（時間分離）** | **1.070** | **+9.98%** | **-10.62%** | **0.940** |
| TOP2_SEC（出来高） | -0.335 | -2.03% | -16.27% | — |

**銘柄選択バイアス**: +0.654 Sharpe
**真の性能推定**: Sharpe ≈ 1.07（Phase 1基準クリア）

#### TOP2_SEC について
- 2020年 DD=-16.27% でCBが発動 → 2021-2024 全ロック
- 戦略の問題ではなくCBデッドロック（entry_stop_only CB で解消見込み）
- 本日の対象外。strategy exploration は現在保留

### レジームブレークダウン
BIASED との差分が「macro regime capture か否か」を確認。

| レジーム | TEMPORAL | BIASED |
|---|---|---|
| 上昇平均（2019/2021/2023） | +23.7% | +34.4% |
| 下落平均（2018/2022/2024） | **-1.3%** | **+2.7%** |

**判定: macro regime capture ではない。**

- TEMPORAL 下落時 -1.3% → 相場依存でない健全なパターン
- 2021年 TEMPORAL +4.1%（BIASED +29.9%）の差 → BIASEDが将来情報で海運・銀行（2021急騰）を先読みしていた証拠。TEMPORALの欠陥ではない

### research_state.md 更新（Research Freeze）
バイアス定量化結果・レジーム判定・Decision を research_state.md に記録。

---

## 判断・Decision

1. **実運用ユニバースを TEMPORAL 選定（24銘柄）に切り替える**（次タスク）
2. macro regime capture の懸念は否定された
3. サバイバルバイアスは残存（影響 ≈ CAGR+1〜2%と推定・許容範囲）

---

## TEMPORALユニバース本番反映（2026-03-19 完了）

**新規ファイル**: `configs/universe/2026Q1_temporal24.json`
**修正ファイル**: `run_live_signal.py` / `.env`

変更内容:
- `LIVE_UNIVERSE_FILE=configs/universe/2026Q1_temporal24.json` を `.env` に追加
- `run_live_signal.py`: `G29_UNIVERSE` ハードコードを廃止し `load_universe()` で動的ロードに変更
- 安全設計追加: MAX_DAILY_ORDERS=20 / MAX_SYMBOL_ORDERS=2 / MAX_OPEN_POSITIONS=10
- `.env` 自動ロード（`python-dotenv` or 手動パース）
- バナーにユニバースバージョン表示追加
- ドライラン確認済み（24銘柄 v=2026Q1_temporal24 ✅）

## Phase 2評価スクリプト完成（2026-03-19）

**新規ファイル**: `scripts/monthly_pnl.py`

**機能**:
- データソース: kabuステーション API（約定照会 GET /orders?product=1&state=5）→ ログ推定価格フォールバック
- FIFO P&L計算（BUYキューから対応コストをマッチング）
- 月次集計（`resample("ME")`）/ 週次異常検知（2σフラグ）
- Phase 2判定: 月¥10,000×3ヶ月連続でクリア
- 未実現P&L: yfinance現在株価で参考表示
- fillsキャッシュ: `logs/live/fills/YYYYMMDD_fills.json`

**動作確認**:
- `--source log`: ✅ 正常終了（データなし → 蓄積中メッセージ）
- `--summary`: ✅ API0件→ログ0件フォールバック正常
- 模擬データ(FIFO計算): ✅ 数値一致（手数料・実現P&L正確）

**使い方**:
```bash
python scripts/monthly_pnl.py             # 月次 + Phase2判定
python scripts/monthly_pnl.py --summary   # 判定サマリーのみ
python scripts/monthly_pnl.py --weekly    # 週次異常検知追加
python scripts/monthly_pnl.py --source log  # ログ強制（API不要）
```

---

## entry_stop v6 開発・凍結決定（2026-03-19）

**スクリプト**: `backtest/portfolio_entry_stop_v6.py`（新規）
**対象**: TEMPORAL 24銘柄ユニバース（2018-2024）

### 開発の経緯

v5 フル 1x でZ-score誤発火（DD=-0.45%でWARNING）が判明。`dd_abs>1%` 下限フィルターを追加したv6を実装・反復改善した。

#### 実装した機能一覧

| 機能 | バリアント | 実装内容 |
|---|---|---|
| 3条件Z-scoreトリガー | v6 | `z<-1.8 AND dd_abs>1% AND velocity<0` |
| 中央値ボラティリティフロア | v6b | `std_eff = max(std_raw, median(std_history))` |
| レジームゲート | v6c | `rolling_std>0.006 AND std_history_len>=252` でのみZ-score有効化 |
| 回帰ベースvelocity | v6c | `np.polyfit(20日equity)[0] / equity[-1]`（分率/日） |
| DD確認日数 | v6c | `dd_confirm_days=3`（自然ステージは3日連続で確定） |
| Exposure調整DD | v6d | `effective_dd = raw_dd / max(avg_exp_60d, 0.25)` |

#### 全バリアント結果

| バリアント | CAGR | Calmar | NORMAL% | avg_scale | 問題点 |
|---|---|---|---|---|---|
| ベースライン | +4.18% | 0.398 | 100% | 1.000 | — |
| v6（3条件） | -0.35% | -0.047 | 16.7% | — | std小さすぎ・誤発火継続 |
| v6b（中央値フロア） | -0.35% | -0.047 | 16.7% | — | bootstrap汚染（初期std_history≈0.002）|
| v6c（regime-gate） | -0.89% | -0.116 | 12.5% | 0.321 | 2018 velocity lock → 7年ロック |
| v6d（exposure調整DD） | -0.45% | -0.120 | 12.3% | 0.125 | ALERT 33x・ALERT%=85.8% |

#### 根本原因

```
TEMPORAL 2018年: avg_exposure = 0.082（91.8%キャッシュ）
→ portfolio DD = -3.7% は「リスク資産損失」ではなく「稼働不足による回復遅延」
→ velocity/z トリガー → WARNING → entry制限 → さらに稼働率低下 → 7年間WARNING継続
→ exposure調整（÷0.25）は逆効果: raw_dd-3.7% → effective_dd-14.97% → 即ALERT
```

年次 avg_exposure（TEMPORAL 1x）:
- 2018: 0.082 ← 根本原因
- 2019: 0.552 / 2020: 0.453 / 2021: 0.596 / 2022: 0.657 / 2023: 0.684 / 2024: 0.574
- 全体: 0.510

### 最終決定: entry_stop を TEMPORAL 1x で完全凍結

**根拠**: entry_stop は十分にデプロイされたポートフォリオ向けのツール。TEMPORAL 1x の低稼働率（初期 avg_exposure=0.082）は前提条件を満たさず、全4バリアントがベースラインを大幅下回った。

**再評価条件**: avg_exposure > 0.45 が安定維持される Phase 3以降

---

## 次のタスク（更新済み）

| 優先度 | タスク |
|---|---|
| **1** | 2025年実運用OOS検証 |
| 保留 | Top2/セクター + entry_stop_only + CB改善 |

---

## 残存バイアス一覧（現時点）

| バイアス | 状態 | 影響推定 |
|---|---|---|
| in-sample銘柄選択 | ✅ 定量化完了（+0.654 Sharpe）。実運用反映待ち | 実運用後は自動解消 |
| サバイバルバイアス | 残存（2024生存銘柄のみ） | CAGR +1〜2% 過大評価 |
| セクターマッピングバイアス | 未検証（フジコ法/平均回帰の振り分けが2018-2024結果を参照） | 小（セクター自体は事前情報） |
