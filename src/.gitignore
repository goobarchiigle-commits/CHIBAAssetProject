# === 機密情報（CLAUDE.md ルール3：絶対にコミット禁止）===
.env
*.env
.env.local
.env.*.local
# テンプレートは共有可（.env.example は除外しない）

# === Python ===
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# === 仮想環境 ===
venv/
.venv/
env/
ENV/

# === データ/ランタイム/ログ ===
# リポジトリ直下の data/・runtime/・logs/ はルート .gitignore（/data/ /runtime/ /logs/ 相当）で除外済み。
# ここで無印パターン（data/ runtime/ logs/）を書くと src/data/・src/runtime/・src/logs/ 配下の
# ソースコード/テストまで無視してしまうため使用しない（2026-07-08 RCA: src/runtime/ 全47ファイルが
# 一度もgit追跡されていなかった不具合の原因）。

# === Jupyter ===
.ipynb_checkpoints/
*.ipynb

# === IDE / OS ===
.vscode/
.idea/
*.DS_Store
Thumbs.db
