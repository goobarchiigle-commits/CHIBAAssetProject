# 研究ログ 2026-03-23

## 作業概要

**Phase**: Phase 2（実運用診断フェーズ）
**目的**: ライブ運用のシグナル供給不足の原因特定と、診断インフラの整備

---

## 実施内容

### 1. パラメータ確定設計の同期（run_live_signal.py）

バックテスト確定値とライブスクリプトの乖離を解消。

**変更内容**:
| パラメータ | 変更前 | 変更後 |
|---|---|---|
| `MAX_POS` | 4 | 3 |
| `TOP_K` | 4 | 3 |
| `max_single_weight` | 未設定（default=0.25） | 0.15 |

### 2. 運用診断ログ追加（signal_bridge.py）

シグナル供給不足のボトルネック特定に必要な指標を日次追記。

**追加ファイル**:
- `logs/diagnostics/metrics.jsonl` — 日次メトリクス（candidates / exposure / blocked_rsr / blocked_breakout / market_above_ma200）
- `logs/diagnostics/rsr_distribution.jsonl` — RSR分布上位20銘柄（閾値最適化用）

**修正バグ**: `rsr=None` のまま `calc_rsr_momentum()` を呼ぶクラッシュを修正（signal_bridge.py:549）

### 3. 3本並行診断テスト

シグナル供給不足の原因を切り分け。

**Step 1: RSR母集団テスト** (`diagnostics/rsr_universe_test.py`)

| コンテキスト | RSR≥75 | RSR≥70 | 最高RSR |
|---|---|---|---|
| 現行42銘柄 | 3銘柄 | 3銘柄 | 95.2 |
| TOPIX300プロキシ | 4銘柄 | 6銘柄 | 95.6 |

→ **ユニバースバイアスは軽微**（+1銘柄のみ）

**Step 2: 市場レジームログ**

```
TOPIX vs 200日MA: +11.89% → 強気相場確認
```
→ **市場弱気は原因ではない**（完全除外）

**Step 3: RSR期間感度テスト** (`diagnostics/rsr_period_test.py`)

| 計算方式 | RSR≥75 | 最高RSR |
|---|---|---|
| IBD式63日（現行） | 3銘柄 | 95.2 |
| 単純42日 | 0銘柄 | 69.0 |
| ブレンド(IBD70%+21d30%) | 2銘柄 | 97.6 |

→ **IBD式が最良。期間変更は効かない**

### 4. 根本原因特定

**価格フィルターが RSR上位銘柄を除外していた**

| 銘柄 | RSR | 状態 |
|---|---|---|
| 6920.T レーザーテック | 95.2 | ¥3,193,000 → 除外継続 |
| 8002.T 丸紅 | 81.0 | ¥515,600 → **¥600,000上限で解消** |
| 8035.T 東京エレクトロン | 78.6 | ¥3,794,000 → 除外継続 |

### 5. 価格フィルター上限引き上げ

**変更**: `MAX_POSITION_YEN` ¥500,000 → ¥600,000

- `.env` と `run_live_signal.py` 両方更新
- 8002.T（丸紅）が評価対象に復帰
- 本日は RSRモメンタム=-9.52（負）のためBUY待機状態

### 6. ファイル整理

- 散在`.log`ファイル → `logs/research/` に集約（6本）
- `analysis/` → `diagnostics/` に統合
- `backtest/archive/` に移動: portfolio_entry_stop_v5/v6（失敗）、rsr_context_sweep、exit_param_3way、topk_sweep、topk_rotation、portfolio_atr_weight、clenow_momentum、sepa_sensitivity、rsr_sensitivity（9本）
- `.env.txt`（重複ファイル）削除
- `research_state.md` のファイル構成図を更新

---

## 次のアクション

1. **10営業日 paper trade 観察**（2026-03-24〜2026-04-08 予定）
   - `logs/diagnostics/metrics.jsonl` を毎朝確認
   - 注目指標: `candidate_count`, `signals_blocked_rsr`

2. **RSR閾値調整の判断基準**
   - `avg_blocked_rsr > 10/day` が続く → min_rsr=70 テスト
   - `candidate_count > 0` が出たら → Donchianのタイミングで自動BUY

3. **8002.T の BUY条件**
   - RSR=81（閾値通過済み）
   - モメンタムが正転換 + 20日高値ブレイクで自動シグナル
