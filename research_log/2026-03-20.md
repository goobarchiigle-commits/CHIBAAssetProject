# 研究ログ 2026-03-20

## 実施内容

### 1. Top-k ローテーション バックテスト（`backtest/topk_rotation.py`）

- k=2,3,4,5 の 4 ケース + stop_loss / max_hold_days バリアントを一括検証
- **採用決定**: k=4 / max_hold_days=60 / stop_loss=None
  - OOS MaxDD -13.98%（Phase 1 基準 -20% をクリア）
  - OOS Sharpe 1.114

### 2. ライブシステム Top-k 本番実装

#### signal_bridge.py（完全リライト）
追加機能:
- `select_top_k()`: RSR上位k銘柄 + 流動性tie-breaker（5B円/日）
- `_trading_days_held()`: 営業日計算（pd.bdate_range）
- `_update_cb_state()`: CB 状態機械（NORMAL/CB_ACTIVE/RECOVERY）
- `_save_cb_event()`: `logs/cb_events/` へ JSONL ログ
- `update_state_after_execution()`: 約定後に entry_date / reentry_blocked 更新
- `_send_orders()`: order_rate_limit=3件/分（20秒/件インターバル）
- `_generate_all_signals()`: top_k override + 時間ストップ判定
- `_build_orders()`: max_new_positions_per_day=2 制限
- `runtime/portfolio_state.json` による状態永続化

#### run_live_signal.py（差分変更）
- `min_rsr=70.0` → `min_rsr=0.0`（top_k で代替）
- `TOP_K=4`, `MAX_HOLD_DAYS=60`, `MAX_NEW_POS_PER_DAY=2` 追加
- 発注後に `bridge.update_state_after_execution()` 呼び出し
- CB状態・Top-k銘柄・保有日数・DD を画面表示

## テスト結果
- 構文チェック: OK（ast.parse）
- `select_top_k` 動作確認: D,A,B（期待通り）
- CB 状態機械: NORMAL→CB_ACTIVE→RECOVERY→NORMAL（全遷移正常）
- `update_state_after_execution`: entry_date 記録・reentry_blocked 設定 OK

## 未解決課題
- `runtime/portfolio_state.json` の初期値が未設定
  → 現在保有 5401.T / 5411.T の entry_date を手動設定する必要あり
- k=4 での avg_exposure 改善量の実測（2025年 OOS で要確認）
- CB `entry_stop_only` 方式との比較（top_k 基盤で再検証必要）
