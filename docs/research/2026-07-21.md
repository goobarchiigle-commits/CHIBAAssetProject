# 日次研究ログ 2026-07-21 — Study82 Phase0=PASS + Study83実装/実測=REJECT

## Study83 — 指数TSMOM実測評価（P0承認済みタスク）

ユーザーASK_FIRST承認済みタスクに従い実装・実行:

1. `src/backtest/study83_tsmom_sleeve.py`新規実装: TOPIX単一指数・絶対モメンタム固定グリッド
   (N=20/60/120d)・vol-target22.5%(Study103 Base仮定流用)・レバ上限2.0x・コスト0.05%/turnover
2. 実行→Core相関がNoneになるバグ発見（Period型と Timestamp型の月次インデックス不一致）→修正→再実行
3. 結果を精査: MaxDD-70%が実装バグでないことを確認するため2020年COVID前後のexposure推移を検証
   →低ボラ局面でのレバ上限張り付き+後方参照型vol-targetのラグを実際に確認（バグではなく実挙動）

## 結果: **REJECT**（本実装スコープ）

canon成功ゲート（Sharpe≥0.8∧Corr<0.5∧コスト後正）0/3アーム通過。最良アーム(N=60)でも
CAGR+2.47%・Sharpe0.10——Study103仮定Conservative(CAGR15%)に-12.5pp未達。MaxDDは仮定を
最大+55pp超過。Core相関のみ仮定より良好（実測-0.26〜+0.18）。PEAD相関はN/A
（意図的未測定・Phase D未実施のため）。

## 判定根拠

REVISEではなくREJECTとした理由: パラメータ調整による改善余地を示唆する含みはFalsification原則4
（No post-result parameter exploration）に抵触するため。3固定グリッドという事前定義範囲内での
結果を、結果を見た後に変更することなくそのまま報告。

## 推奨（未実行・advisory）

TSMOM assumptions downgraded→Study103 assumptions rerun（§8A-4A規則=1回のみ）→Route B frontier
re-estimationが技術的に整合的な次ステップだが、追加ガバナンス変更/ポートフォリオ再最適化の禁止
事項に該当するため本タスクでは実行していない。

成果物: `reports/study83_tsmom_sleeve.md`。roadmap_v15をv1.5.9へ改版（実測反映）。
Unknown#2解消。残りはUnknown#3（Core真の能力・CP3本体）のみ。

---

# （前半）Study82 Phase0実行完了・PASS確定（実行日2026-07-20分の続き）

## Study82 Phase0 — 決算発表日時精度監査（実データ検証）

ユーザーのASK_FIRST承認済みタスク仕様に従い実装・実行:

1. `src/jquants/provider.py::get_fins_summary()`新規実装（`/v2/fins/summary`エンドポイント・
   raw response保存・client.py既存のretry/backoff機構を利用）
2. `src/scripts/study82_phase0_audit.py`新規実装（Audit1-6・小サンプルn=83・流動性大型株1銘柄+
   廃止済み実銘柄1銘柄）
3. 実行→初回はAudit4 FAILだったが、実データのフィールド名が`TypeOfDocument`/`DisclosureNumber`
   ではなく実際は`DocType`/`DiscNo`（略記）と判明・候補リスト修正→再実行

## 結果: **PASS**（Audit1-6全PASS）

- DiscTime: 12種の時刻値（08:30-15:38）・場中50件/引後33件を機械判別可能——定型値ではない
- DocType+DiscNo: 訂正/修正開示を構造上区別可能。実データにも修正開示13件実在
  （EarnForecastRevision）
- Delisted coverage: 実在廃止銘柄（コード44490・2026-06-29上場廃止）で29件取得

## 留保事項（正直に記録）

1. `from`/`to`パラメータが本エンドポイントで機能していない疑い（1コード指定のみで約10年分
   全履歴が返った）——Phase D大規模収集時の設計に影響
2. Audit4で確認できた修正開示は業績予想修正のみ・決算数値自体の訂正は本サンプル未確認
3. DiscTime表記の不統一（"12:00"/"12:00:00"）
4. Audit6は単一銘柄プローブ（母集団欠落率未測定）

## 決定木の適用

`PASS → Study103 rerun unnecessary → Proceed only to Study83 Proposal`。
Research Freeze Rule解除条件成立。Study83実装への着手は別途ユーザー判断。

成果物: `reports/study82_phase0_report.md`。roadmap_v15をv1.5.8へ改版（実測反映）。
