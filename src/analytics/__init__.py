"""src/analytics — Deployable-alpha leakage analytics (read-only, analytics-only).

Modules:
  skipped_opportunity_analytics      — SkippedOpportunityRecord tracking
  deployable_alpha_metrics           — aggregate leakage metrics
  daily_leakage_summary              — deterministic daily summaries
  alpha_diagnostic                   — capital-efficiency diagnostic snapshots + rankings
  research_priority_summary          — deterministic daily research-priority reports
  forward_attribution_validator      — fail-closed forward attribution integrity validator
"""
